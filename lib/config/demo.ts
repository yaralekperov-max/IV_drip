/**
 * Демо-режим: показывать переключатель состояний ЛК («новый/активный клиент») и прочие
 * демонстрационные элементы. В проде выключается: NEXT_PUBLIC_DEMO_MODE=false.
 *
 * По умолчанию (переменная не задана) — включён (dev/демо-деплой).
 */
export const DEMO_MODE = process.env.NEXT_PUBLIC_DEMO_MODE !== "false";
